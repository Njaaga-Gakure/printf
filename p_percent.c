#include "main.h"
/**
 * p_percent - prints out a percent symbol
 * @list: argument list
 * Return: return number of the character printed out
 */

int p_percent(__attribute__((unused))va_list list)
{
	_putchar('%');
	return (1);
}
